// BlackVaultOffchainResolver.sol
// [ REDACTED - source-available release ]
// ENSIP-10 offchain resolver: resolve() reverts with OffchainLookup(url),
// resolveWithProof() verifies the gateway's signed answer against an
// allowlisted signer set. Deployed and verified on-chain.
